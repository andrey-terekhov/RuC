notifications:
  email: false

git:
  depth: 1

language: c

matrix:
  include:
    - name: "macOS 10.15.5"
      os: osx
      osx_image: xcode12
    - name: "Ubuntu 18.04"
      os: linux
      dist: bionic

script:
  - ./scripts/test.sh -v v1.1
