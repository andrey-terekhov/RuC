void main()
{
    int i;
    char c = '*';
	i = c == '*' ? 0 : c == '/' ? 1 : c == '+' ? 2 : c == '-' ? 3 : -1;
    print(i);  // 0
    c = '-';
    print(-(c == '*' ? 0 : c == '/' ? 1 : c == '+' ? 2 : c == '-' ? 3 : -1) + 1);  // -2
    
}

