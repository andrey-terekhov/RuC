void main()
{
    int i;
    char c = '*';
	i = c == '*' ? 0 : c == '/' ? 1 : c == '+' ? 2 : c == '-' ? 3 : -1;
    //print(i);  // 0
    assert(i == 0, "Must be 0");
    c = '-';
    //print(-(c == '*' ? 0 : c == '/' ? 1 : c == '+' ? 2 : c == '-' ? 3 : -1) + 1);  // -2
    assert((-(c == '*' ? 0 : c == '/' ? 1 : c == '+' ? 2 : c == '-' ? 3 : -1) + 1) == -2, "Must be -2");
}

